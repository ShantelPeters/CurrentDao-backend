import { Repository } from 'typeorm';
import { FraudCaseEntity } from '../entities/fraud-case.entity';
import { FraudMlService } from '../ml/fraud-ml.service';
import { PatternRecognitionService } from '../patterns/pattern-recognition.service';
import { SuspiciousActivityService } from '../reporting/suspicious-activity.service';
import { FraudPreventionService } from '../prevention/fraud-prevention.service';
import { AnalyzeTradeDto, FraudAnalysisResult } from '../dto/fraud-alert.dto';
export declare class RealTimeMonitorService {
    private readonly fraudCaseRepository;
    private readonly mlService;
    private readonly patternService;
    private readonly reportingService;
    private readonly preventionService;
    private readonly logger;
    private readonly monitoredTraders;
    private readonly pendingTrades;
    private readonly CONTEXT_WINDOW_SIZE;
    private readonly MONITORING_INTERVAL_MS;
    private readonly ALERT_THRESHOLD;
    private monitoringInterval;
    constructor(fraudCaseRepository: Repository<FraudCaseEntity>, mlService: FraudMlService, patternService: PatternRecognitionService, reportingService: SuspiciousActivityService, preventionService: FraudPreventionService);
    analyzeIncomingTrade(tradeDto: AnalyzeTradeDto): Promise<FraudAnalysisResult>;
    startTraderMonitoring(traderId: string): void;
    stopTraderMonitoring(traderId: string): void;
    startGlobalMonitoring(): void;
    stopGlobalMonitoring(): void;
    getMonitoringStatus(): object;
    processMonitoringCycle(): Promise<void>;
    generateMonitoringReport(): Promise<void>;
    hourlyEscalation(): Promise<void>;
    private sweepTrader;
    private updateTraderContext;
    enqueuePendingTrade(tradeDto: AnalyzeTradeDto): void;
    private getTraderContext;
    private combineScores;
    private scoreToSeverity;
    private getRecommendedAction;
    private generateCaseId;
    private createFraudCase;
    onModuleDestroy(): void;
}
